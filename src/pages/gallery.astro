---
import MainLayout from "../layouts/MainLayout.astro";
import GalleryLightbox from "../components/GalleryLightbox";
import { galleryPhotos } from "../data/gallery";
import { withBase } from "../utils/with-base";

const base = import.meta.env.BASE_URL;
const photos = galleryPhotos.map((photo) => ({
  ...photo,
  original: withBase(photo.original, base),
  src: withBase(photo.src, base),
  thumb: withBase(photo.thumb, base),
}));
---

<MainLayout
  title="相簿"
  description="以滑動為主的婚紗相簿，手機與桌機都能舒服瀏覽。"
>
  <section class="gallery-header">
    <div>
      <p class="section-label">相簿</p>
      <h1>一張一張慢慢看，把每個瞬間留久一點。</h1>
    </div>
    <p class="gallery-intro">
      手機可左右滑動，桌機可用方向鍵或下方縮圖切換；點照片可打開原始畫質。
    </p>
  </section>

  <GalleryLightbox client:load photos={photos} />
</MainLayout>
